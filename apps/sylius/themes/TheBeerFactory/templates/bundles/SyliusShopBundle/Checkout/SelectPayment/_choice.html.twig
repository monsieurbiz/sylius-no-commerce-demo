<div class="choice flex flex-main-between w-full mb-md" {{ sylius_test_html_attribute('payment-item') }}>
    <div class="flex-item-fluid">
        {{ form_row(form, sylius_test_form_attribute('payment-method-checkbox')) }}
        {% if method.description is not null %}
            <p class="mt-xxs mb-n pl-xl text-xs text-tight">{{ method.description }}</p>
        {% endif %}
    </div>
</div>
